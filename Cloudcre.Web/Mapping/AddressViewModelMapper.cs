
namespace Cloudcre.Web.Mapping
{
    public static class AddressViewModelMapper
    {
        //public static IEnumerable<SelectListItem> ConvertToSelectListItemEnumerable(this AddressCountryType addressCountryType)
        //{
        //    return ConvertToEnumerable(addressCountryType);
        //}

        //private static IEnumerable<SelectListItem> ConvertToEnumerable<T>(this T enumType)
        //{
        //    IEnumerable<T> obj = Enum.GetValues(typeof(T)).Cast<T>();
        //    IEnumerable<SelectListItem> selectList = Mapper.Map<IEnumerable<T>, IEnumerable<SelectListItem>>(obj).ToList();

        //    foreach (var item in selectList.Where(item => int.Parse(item.Value) == (int)Convert.ChangeType(enumType, typeof(int))))
        //    {
        //        item.Selected = true;
        //    }

        //    return selectList;
        //}
    }
}