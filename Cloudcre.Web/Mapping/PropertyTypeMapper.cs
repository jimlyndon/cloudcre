using System;
using System.Collections.Generic;
using System.Linq;
using System.Web.Mvc;
using AutoMapper;
using Cloudcre.Model;

namespace Cloudcre.Web.Mapping
{
    public static class PropertyTypeMapper
    {
    //    public static IEnumerable<SelectListItem> ConvertToSelectListItemEnumerable(this PropertyType propertyType)
    //    {
    //        IEnumerable<PropertyType> obj = Enum.GetValues(typeof(PropertyType)).Cast<PropertyType>();
    //        IEnumerable<SelectListItem> selectList = Mapper.Map<IEnumerable<PropertyType>, IEnumerable<SelectListItem>>(obj).ToList();

    //        foreach (var item in selectList.Where(item => int.Parse(item.Value) == (int)propertyType))
    //        {
    //            item.Selected = true;
    //        }

    //        return selectList;
    //    }
    }
}