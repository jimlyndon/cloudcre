@model Cloudcre.Service.ViewModels.MultipleFamilyViewModel

@{
    ViewBag.Title = "Cloud CRE - Create new multiple-family property";
    Layout = "~/Views/Shared/Property/_Layout_Property_Create.cshtml";
}

@section scripts {
    <script type="text/javascript">
        window.cloudcre.routing.urls({
            action: '@Url.Action("Create", "MultipleFamily")',
            model: '@Url.Action("Model", "MultipleFamily")'
        });
    </script>
    <script type="text/html" id="template">
        @using (Html.BeginForm("Create", "MultipleFamily", FormMethod.Post, new { id = "dialog-form", @class = "wufoo" }))
        {
            @Html.Partial("_Template")
        }
    </script>
}