@model Cloudcre.Service.Property.ViewModels.MultipleFamilyViewModel

@{
    ViewBag.Title = "Cloud CRE - Edit multiple-family property";
    Layout = "~/Views/Shared/Property/_Layout_Property_Edit.cshtml";
}

@section scripts {
    <script type="text/javascript">
        window.cloudcre.routing.urls({
            action: '@Url.Action("Edit", "MultipleFamily")',
            model: '@Url.Action("Model", "MultipleFamily", new { Model.Id })'
        });
    </script>
    <script type="text/html" id="template">
        @using (Html.BeginForm("Edit", "MultipleFamily", FormMethod.Post, new { id = "dialog-form", @class = "wufoo" }))
        {
            @Html.HiddenFor(model => model.Id)
            @Html.Partial("_Template")
        }
    </script>
}