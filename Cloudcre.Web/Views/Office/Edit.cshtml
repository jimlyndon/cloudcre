@model Cloudcre.Service.ViewModels.OfficeViewModel

@{
    ViewBag.Title = "Cloud CRE - Edit office property";
    Layout = "~/Views/Shared/Property/_Layout_Property_Edit.cshtml";
}

@section scripts {
    <script type="text/javascript">
        window.cloudcre.routing.urls({
            action: '@Url.Action("Edit", "Office")',
            model: '@Url.Action("Model", "Office", new { Model.Id })'
        });
    </script>
    <script type="text/html" id="template">
        @using (Html.BeginForm("Edit", "Office", FormMethod.Post, new { id = "dialog-form", @class = "wufoo" }))
        {
            @Html.HiddenFor(model => model.Id)
            @Html.Partial("_Template")
        }
    </script>
}