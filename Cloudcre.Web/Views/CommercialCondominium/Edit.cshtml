@model Cloudcre.Service.Property.ViewModels.CommercialCondominiumViewModel

@{
    ViewBag.Title = "Cloud CRE - Edit commercial condo property";
    Layout = "~/Views/Shared/Property/_Layout_Property_Edit.cshtml";
}

@section scripts {
    <script type="text/javascript">
        window.cloudcre.routing.urls({
            action: '@Url.Action("Edit", "CommercialCondominium")',
            model: '@Url.Action("Model", "CommercialCondominium", new { Model.Id })'
        });
    </script>
    <script type="text/html" id="template">
        @using (Html.BeginForm("Edit", "CommercialCondominium", FormMethod.Post, new { id = "dialog-form", @class = "wufoo" }))
        {
            @Html.HiddenFor(model => model.Id)
            @Html.Partial("_Template")
        }
    </script>
}