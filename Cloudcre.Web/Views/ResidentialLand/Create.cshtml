@model Cloudcre.Service.ViewModels.ResidentialLandViewModel

@{
    ViewBag.Title = "Cloud CRE - Create new residential land property";
    Layout = "~/Views/Shared/Property/_Layout_Property_Create.cshtml";
}

@section scripts {
    <script type="text/javascript">
        window.cloudcre.routing.urls({
            action: '@Url.Action("Create", "ResidentialLand")',
            model: '@Url.Action("Model", "ResidentialLand")'
        });
    </script>
    <script type="text/html" id="template">
        @using (Html.BeginForm("Create", "ResidentialLand", FormMethod.Post, new { id = "dialog-form", @class = "wufoo" }))
        {
            @Html.Partial("_Template")
        }
    </script>
}