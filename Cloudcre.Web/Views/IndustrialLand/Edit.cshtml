@model Cloudcre.Service.Property.ViewModels.IndustrialLandViewModel

@{
    ViewBag.Title = "Cloud CRE - Edit industrial land property";
    Layout = "~/Views/Shared/Property/_Layout_Property_Edit.cshtml";
}

@section scripts {
    <script type="text/javascript">
        window.cloudcre.routing.urls({
            action: '@Url.Action("Edit", "IndustrialLand")',
            model: '@Url.Action("Model", "IndustrialLand", new { Model.Id })'
        });
    </script>
    <script type="text/html" id="template">
        @using (Html.BeginForm("Edit", "IndustrialLand", FormMethod.Post, new { id = "dialog-form", @class = "wufoo" }))
        {
            @Html.HiddenFor(model => model.Id)
            @Html.Partial("_Template")
        }
    </script>
}