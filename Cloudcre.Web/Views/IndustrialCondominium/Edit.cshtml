@model Cloudcre.Service.Property.ViewModels.IndustrialCondominiumViewModel

@{
    ViewBag.Title = "Cloud CRE - Edit industrial condo property";
    Layout = "~/Views/Shared/Property/_Layout_Property_Edit.cshtml";
}

@section scripts {
    <script type="text/javascript">
        window.cloudcre.routing.urls({
            action: '@Url.Action("Edit", "IndustrialCondominium")',
            model: '@Url.Action("Model", "IndustrialCondominium", new { Model.Id })'
        });
    </script>
    <script type="text/html" id="template">
        @using (Html.BeginForm("Edit", "IndustrialCondominium", FormMethod.Post, new { id = "dialog-form", @class = "wufoo" }))
        {
            @Html.HiddenFor(model => model.Id)
            @Html.Partial("_Template")
        }
    </script>
}