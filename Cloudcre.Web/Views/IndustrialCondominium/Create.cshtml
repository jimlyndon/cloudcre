@model Cloudcre.Service.Property.ViewModels.IndustrialCondominiumViewModel

@{
    ViewBag.Title = "Cloud CRE - Create new industrial condo property";
    Layout = "~/Views/Shared/Property/_Layout_Property_Create.cshtml";
}

@section scripts {
    <script type="text/javascript">
        window.cloudcre.routing.urls({
            action: '@Url.Action("Create", "IndustrialCondominium")',
            model: '@Url.Action("Model", "IndustrialCondominium")'
        });
    </script>
    <script type="text/html" id="template">
        @using (Html.BeginForm("Create", "IndustrialCondominium", FormMethod.Post, new { id = "dialog-form", @class = "wufoo" }))
        {
            @Html.Partial("_Template")
        }
    </script>
}