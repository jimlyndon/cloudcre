@model Cloudcre.Service.Property.ViewModels.CommercialLandViewModel

@{
    ViewBag.Title = "Cloud CRE - Edit commercial land property";
    Layout = "~/Views/Shared/Property/_Layout_Property_Edit.cshtml";
}

@section scripts {
    <script type="text/javascript">
        window.cloudcre.routing.urls({
            action: '@Url.Action("Edit", "CommercialLand")',
            model: '@Url.Action("Model", "CommercialLand", new { Model.Id })'
        });
    </script>
    <script type="text/html" id="template">
        @using (Html.BeginForm("Edit", "CommercialLand", FormMethod.Post, new { id = "dialog-form", @class = "wufoo" }))
        {
            @Html.HiddenFor(model => model.Id)
            @Html.Partial("_Template")
        }
    </script>
}