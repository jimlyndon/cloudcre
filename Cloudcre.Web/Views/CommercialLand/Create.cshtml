@model Cloudcre.Service.Property.ViewModels.CommercialLandViewModel

@{
    ViewBag.Title = "Cloud CRE - Create new commercial land property";
    Layout = "~/Views/Shared/Property/_Layout_Property_Create.cshtml";
}

@section scripts {
    <script type="text/javascript">
        window.cloudcre.routing.urls({
            action: '@Url.Action("Create", "CommercialLand")',
            model: '@Url.Action("Model", "CommercialLand")'
        });
    </script>
    <script type="text/html" id="template">
        @using (Html.BeginForm("Create", "CommercialLand", FormMethod.Post, new { id = "dialog-form", @class = "wufoo" }))
        {
            @Html.Partial("_Template")
        }
    </script>
}